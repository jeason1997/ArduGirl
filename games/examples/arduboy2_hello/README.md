# Arduboy2 官方 HelloWorld 示例

实际游戏源码来自 `third_party/Arduboy2/examples/HelloWorld/HelloWorld.ino`，固定在 Arduboy2 commit `bc460a2cff1a3e116880991aa2f88bae4b2e3160`。

ArduGirl 不修改该 `.ino` 文件。`entry.cpp` 只负责把 Arduino sketch 纳入普通 C++ 构建，平台适配由 `include/Arduboy2.h` 和 ArduGirl 实现完成。

